sudo: false
language: python
python:
  - "3.6"
install:
  - pip install -r ci/python/requirements.txt
script:
  - bash ci/pylint/get-badge.sh
  - pytest --cov=ndna/ tests/
after_success:
  - codecov
notifications:
  email: false
